parent_config: https://gist.githubusercontent.com/RISCfuture/c57d132e5e0160a768df1625e20ae3b8/raw/.swiftlint.yml

excluded:
  - DerivedData
  - build

disabled_rules:
  # Nimble is not a dependency; the tests use XCTest and Swift Testing.
  - prefer_nimble
